<odoo>
    <record id="view_crm_lead_form_site_visits" model="ir.ui.view">
        <field name="name">crm.lead.form.site.visits</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_site_visits"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-calendar">
                    <field name="site_visit_count" widget="statinfo" string="Site Visits"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>